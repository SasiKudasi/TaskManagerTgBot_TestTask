---
meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  dockerfile: Dockerfile
  skip: true
run:
  persistenceMount: /data
  containerPort: 80
  image: mcr.microsoft.com/dotnet/runtime:8.0
  command: 1>docker build -f "./Dockerfile" --force-rm -t taskmanagertgbot  --build-arg
    "BUILD_CONFIGURATION=Release" --label "com.microsoft.created-by=visual-studio"
    --label "com.microsoft.visual-studio.project-name=TaskManagerTgBot" "/TaskManagerTgBot"
