---
meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  dockerfile: Dockerfile
  skip: true
run:
  persistenceMount: /data
  containerPort: 80
  image: mcr.microsoft.com/dotnet/runtime:8.0
